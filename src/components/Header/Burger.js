import React from 'react'

const Burger = () => {
  return (
    <button className="burger" style={{display: 'none'}}>
      <i className="material-icons">menu</i>
    </button>
  )
}

export default Burger
